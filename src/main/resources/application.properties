spring.application.name=booking-service

spring.jpa.hibernate.ddl-auto=update

spring.datasource.username=${MYSQLUSER:backend1}
spring.datasource.password=${MYSQLPASSWORD:12345}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.datasource.url=jdbc:${MYSQL_URL:mysql://localhost:3306/pensionat}
server.port=${MYSQLPORT:8081}

customer.url=${CUSTOMER_URL:http://customerservice:8081}